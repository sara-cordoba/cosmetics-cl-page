import { PipeCopyrightPipe } from './pipe-copyright.pipe';

describe('PipeCopyrightPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCopyrightPipe();
    expect(pipe).toBeTruthy();
  });
});
